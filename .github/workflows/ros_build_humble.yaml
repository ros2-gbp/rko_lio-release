name: Humble
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build_ros_humble:
    uses: ./.github/workflows/ros_build_reusable.yaml
    with:
      ros_distro: humble
